# MXD Automation 精简版设计说明

## 1. 改造目标

当前项目包含较多自动化功能，例如：

* 自动补血 / 回蓝
* 自动拾取
* 爬绳
* 上下平台移动
* 跨平台路径规划
* 多种复杂状态切换
* 多层地图理解

这些功能会显著增加状态机、路径规划和异常处理的复杂度。

本次精简的目标是：

> 将项目改造成一个只负责“单个平台水平移动、追怪、攻击”的轻量级自动战斗辅助工具。

核心原则：

**只处理当前水平平台，不尝试理解整张地图。**

---

## 2. 保留功能

精简后只保留以下几个核心能力。

### 2.1 玩家位置识别

系统需要识别玩家当前所在位置。

主要关注：

* 玩家 X 坐标
* 玩家 Y 坐标

其中 X 坐标主要用于水平移动判断。

Y 坐标主要用于判断怪物是否与玩家处于同一平台。

示例：

```text
player_x = 520
player_y = 430
```

---

### 2.2 怪物识别

继续使用现有视觉检测能力识别怪物。

需要获取：

```text
monster_x
monster_y
```

系统不需要处理所有怪物。

只处理与玩家 Y 坐标接近的怪物。

例如：

```python
abs(monster_y - player_y) < PLATFORM_Y_THRESHOLD
```

满足条件时，可以认为怪物处于玩家当前水平平台。

这样可以避免处理：

* 上层怪物
* 下层怪物
* 需要爬绳才能到达的怪物
* 需要跳跃或下落才能到达的怪物

---

## 3. 核心行为

整个系统只保留三个核心行为：

```text
PATROL
CHASE
ATTACK
```

---

## 4. PATROL：水平巡逻

当当前平台没有检测到有效怪物时：

玩家在平台上左右移动。

例如：

```text
向右移动
    ↓
达到平台边界
    ↓
向左移动
    ↓
达到平台边界
    ↓
再次向右移动
```

形成简单的水平巡逻。

暂时不考虑：

* 跳跃
* 下平台
* 爬绳
* 主动寻找其他楼层怪物

---

## 5. CHASE：追怪

当发现同平台怪物时：

计算玩家和怪物之间的水平距离。

```python
distance_x = monster_x - player_x
```

如果：

```text
monster_x < player_x
```

则：

```text
按住 LEFT
```

如果：

```text
monster_x > player_x
```

则：

```text
按住 RIGHT
```

玩家持续向目标怪物靠近。

---

## 6. ATTACK：攻击

当玩家与怪物的水平距离小于攻击距离时：

```python
abs(monster_x - player_x) < ATTACK_RANGE
```

进入攻击状态。

例如：

```text
ATTACK_RANGE = 120
```

此时：

```text
停止移动
↓
执行攻击
```

攻击结束后重新进行怪物检测。

如果怪物仍然存在：

```text
继续攻击
```

如果怪物消失：

```text
重新寻找目标
```

如果没有怪物：

```text
进入 PATROL
```

---

## 7. 最小决策逻辑

整个决策系统可以简化为：

```python
if monster_exists:

    if monster_is_on_same_platform:

        if monster_is_in_attack_range:
            attack()

        else:
            move_towards(monster)

else:
    patrol()
```

相比复杂 FSM，这种方式更容易：

* 调试
* 修改
* 扩展
* 查看问题
* 控制行为

---

## 8. 可以删除的功能

本次精简建议移除以下功能。

### 自动补血 / 回蓝

删除：

```text
HEALING
```

以及：

* HP 检测
* MP 检测
* 药水快捷键
* 血量阈值
* 蓝量阈值
* 自动恢复逻辑

---

### 自动拾取

删除所有：

* 掉落物检测
* 自动拾取
* 掉落物路径判断
* 拾取状态

原则：

```text
怪物死亡
↓
直接寻找下一只怪物
```

不主动返回拾取掉落物。

---

### 爬绳

删除：

```text
CLIMBING
```

以及：

* 绳索检测
* 绳索位置计算
* 爬绳动作
* 上下平台切换

---

### 下平台

删除：

```text
DROPPING
```

不再主动：

* 跳下平台
* 下跳
* 判断下一层平台

---

### 跨平台路径规划

删除复杂地图路径逻辑。

不再判断：

```text
玩家在哪一层
怪物在哪一层
最近的绳索在哪里
如何去其他楼层
```

系统只认识：

```text
当前平台
```

---

## 9. 精简后的世界模型

原来的系统可能需要理解：

```text
整个地图

平台 A
├── 怪物
├── 绳索
└── 掉落物

平台 B
├── 怪物
└── 绳索

平台 C
└── 怪物
```

精简后：

```text
←──────────── 当前平台 ────────────→

怪物        玩家          怪物

             ↑
         只处理这一层
```

---

## 10. 推荐状态结构

如果仍然保留 FSM：

```python
class State(Enum):
    PATROL = 1
    CHASE = 2
    ATTACK = 3
```

状态关系：

```text
         没怪
     ┌──────────┐
     ↓          │
  PATROL        │
     │          │
发现怪物        │
     ↓          │
   CHASE        │
     │          │
进入攻击距离     │
     ↓          │
   ATTACK ──────┘
     │
怪物仍存在
     ↓
继续 ATTACK / CHASE
```

---

## 11. 更推荐的方案：弱化 FSM

如果实际逻辑足够简单，可以不使用完整状态机。

直接由每一帧的 Context 决定行为：

```python
def decide(context):

    monster = find_target_monster(context)

    if monster is None:
        return PATROL

    distance = monster.x - context.player.x

    if abs(distance) <= ATTACK_RANGE:
        return ATTACK

    return CHASE
```

这种模式对于当前需求已经足够。

---

## 12. 推荐核心模块

精简版可以最终只保留以下几个部分：

```text
Screen Capture
     ↓
Perception
     ↓
Player Detection
Monster Detection
     ↓
Context
     ↓
Decision
     ↓
PATROL / CHASE / ATTACK
     ↓
Action Executor
     ↓
Keyboard Input
```

---

## 13. 推荐目录结构

后续可以逐渐简化为：

```text
src/

├── perception/
│   ├── player_detector.py
│   └── monster_detector.py
│
├── decision/
│   ├── context.py
│   └── decision_engine.py
│
├── execution/
│   └── action_executor.py
│
├── config/
│   └── settings.py
│
└── main.py
```

不再需要复杂的：

```text
navigation
rope
healing
loot
platform_path
multi_floor
drop
```

模块。

---

## 14. 第一阶段目标

第一阶段只实现：

```text
截图
↓
识别玩家
↓
识别当前平台怪物
↓
选择最近怪物
↓
向怪物方向水平移动
↓
进入攻击距离
↓
攻击
↓
怪物死亡
↓
寻找下一只
```

如果没有怪物：

```text
左右巡逻
```

第一阶段不做任何其他功能。

---

## 15. 后续可选扩展

当单平台版本稳定以后，再考虑增加：

### 可选 1：自动转向

攻击前自动面对怪物。

---

### 可选 2：攻击技能轮转

例如：

```text
普通攻击
技能 1
技能 2
```

但仍然保持单平台逻辑。

---

### 可选 3：更稳定的平台判断

通过：

* 怪物脚底位置
* 玩家脚底位置
* 地板检测结果

判断是否处于同一平台。

---

### 可选 4：卡住检测

例如：

```text
持续按 RIGHT
但玩家 X 坐标 1 秒没有变化
```

则认为角色被障碍卡住。

可以：

```text
停止
反向移动
重新寻找目标
```

---

## 16. 暂时不要增加的功能

为了保持项目简单，建议当前版本明确不做：

```text
自动补血
自动回蓝
自动拾取
自动爬绳
跳跃寻路
跨平台追怪
自动换地图
自动回城
自动补给
复杂导航
完整地图建模
```

---

## 17. 最终定位

项目最终可以定义为：

> 一个基于视觉识别的单平台自动战斗辅助工具。
>
> 系统通过游戏画面识别玩家与怪物，仅处理当前水平平台内的目标，并完成水平巡逻、追怪和攻击。
>
> 项目不处理补血、回蓝、拾取、绳索、跨平台导航和完整地图路径规划，以保持整体逻辑简单、稳定且易于调试。

核心原则：

```text
Keep It Simple.

只看当前平台。
只找怪。
只移动。
只攻击。
```
